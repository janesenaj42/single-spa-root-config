# Infrastructure: .husky/pre-commit

# Infrastructure: .husky/pre-commit

**Type:** shell

## Overview

`.husky/pre-commit` is an infrastructure file (shell). Its behaviour is not derivable from structure, so the source is reproduced in full.




## Source

```shell
#!/usr/bin/env sh

if ! command -v npm >/dev/null 2>&1; then
  echo "husky (pre-commit): npm not found on PATH, skipping lint/test." >&2
  exit 0
fi

npm run lint && npm test

```

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*