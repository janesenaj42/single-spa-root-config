# File: examples/demo-mfes/navbar/navbar.js

# examples/demo-mfes/navbar/navbar.js

## Overview

Docs & Tooling module navbar defining jitter. `examples/demo-mfes/navbar/navbar.js` is a javascript source file in the Docs & Tooling layer.

## Usage Notes

**Layer:** Docs & Tooling | **Role:** entry point into its layer

## In the code

execute bootstrap mount padding unmount admin wh-navbar wh-brand wh-link /assets wh-blueprint 0 0 144 64 144 aria-label= y= width= height= fill= stroke= wh-worker ${w.id} ${w.x} ${w.y} wh-spacer <button id="wh-admin-toggle" class="wh-btn">Admin</button> wh-auth-btn wh-btn wh-btn-primary Logout Login #wh-auth-btn click #wh-admin-toggle warehouse:admin-panel:toggle system register function exports let interval const workers jitter value min max amount return math random async props dom element getter authed authenticated has any role inner html style navbar display flex align items center gap background color e5e7eb font family sans serif brand size letter spacing link c5fd text decoration none blueprint border radius worker transition ease out spacer btn f2937 solid cursor pointer primary nav class strong warehouse ops href assets svg view box width height aria label floor plan locations rect fill f172a stroke map circle data d3ee join span button toggle auth query selector add event listener else publish panel set for each dot attribute fixed clear

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*