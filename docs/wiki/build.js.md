# File: build.js

# build.js

## Overview

Application module build (2 symbols). `build.js` is a javascript source file in the Application layer.

## Usage Notes

**Layer:** Application | **Role:** entry point into its layer

## In the code

esbuild node:fs node:path node:url dist src/root-config.js dist/root-config.js esm es2020 production public index.html silent-check-sso.html import from node path file url const dirname meta dir join mkdir sync recursive true build entry points src root config bundle outfile format target minify process env copy index html silent check sso console log bundled and copied static files

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*