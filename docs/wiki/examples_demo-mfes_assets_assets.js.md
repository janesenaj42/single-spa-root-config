# File: examples/demo-mfes/assets/assets.js

# examples/demo-mfes/assets/assets.js

## Overview

Docs & Tooling module assets defining ensureAgGridLoaded, mockAssets. `examples/demo-mfes/assets/assets.js` is a javascript source file in the Docs & Tooling layer.

## Usage Notes

**Layer:** Docs & Tooling | **Role:** entry point into its layer

## In the code

id name category aisle status execute bootstrap mount unmount script Failed to load ag-grid-community from CDN Forklift Pallet Jack Conveyor Scanner Racking Unit In Use Idle Maintenance color:#e2e8f0;margin:0 0 12px; wh-assets-grid height:520px;width:100%; #wh-assets-grid , headerName: lastChecked Last Checked system register function exports ensure grid loaded window return promise resolve loading new reject const document create element src https cdn jsdelivr net npm community dist min onload onerror error failed load from head append child categories pallet jack racking unit statuses use mock assets rows for let push ast string pad start length last checked date now iso slice api async props dom getter style css text display block height padding box sizing border background b1220 font family sans serif inner html color e2e8f0 margin asset management div width await query selector row data column defs field header flex default col def sortable true filter resizable destroy undefined

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*