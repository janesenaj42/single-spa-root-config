# File: examples/demo-mfes/admin-panel/admin-panel.js

# examples/demo-mfes/admin-panel/admin-panel.js

## Overview

Docs & Tooling module admin-panel defining applyState. `examples/demo-mfes/admin-panel/admin-panel.js` is a javascript source file in the Docs & Tooling layer.

## Usage Notes

**Layer:** Docs & Tooling | **Role:** entry point into its layer

## In the code

execute bootstrap mount background transform unsubscribe unmount wh-drawer-open wh-admin-close Close admin panel wh-admin-row wh-maint-toggle checkbox wh-admin-btn wh-flush-cache wh-flush-status font-size:12px;color:#9ca3af;min-height:16px; padding-left:18px;font-size:14px;line-height:1.8; color:#22c55e; color:#f59e0b; #wh-admin-close click #wh-flush-cache #wh-flush-status warehouse:admin-panel:toggle ${el.id} { position: fixed; top: 0; right: 0; width: 25%; min-width: 280px; height: 100vh; ${el.id}.wh-drawer-open { transform: translateX(0); } system register function exports let open false apply state class list toggle drawer return async props const dom element getter inner html style position fixed top right width min height color e5e7eb box shadow rgba translate transition ease out index font family sans serif sizing border padding overflow auto admin row display flex justify content space between align items center margin btn fff none radius cursor pointer close size float button aria label panel times warehouse div for maint maintenance mode input type span location cache flush status ca3af left line scanner network strong c55e online conveyor belt f59e0b degraded cold

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*