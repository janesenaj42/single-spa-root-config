# File: src/containers.js

# src/containers.js

## Overview

`src/containers.js` is a javascript source file in the Application layer.

It exposes 2 public symbols.

## Public API

| Symbol | Kind | Signature |
| --- | --- | --- |
| `parseSimpleSelector` | function | function parseSimpleSelector(selector) |
| `getOrCreateContainer` | function | function getOrCreateContainer(selector, doc = document) |

## Used by

Imported by 2 files in this repository.

**`src`**

- `src/root-config.js`

**`test`**

- `test/containers.test.js`

## Usage Notes

**Layer:** Application | **Role:** internal to its layer

## Questions this page answers

- What does `src/containers.js` export?
- Where is `parseSimpleSelector` defined?
- What imports `src/containers.js`?

## In the code

) ? : selector.startsWith( ${selector} div const simple token export function parse selector type starts with class undefined name slice test throw new error auto created containers only support selectors got return returns the element matching creating and appending one doc body doesn already exist see adr get create container document existing query else append child

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*