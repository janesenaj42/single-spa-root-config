# File: src/root-config.js

# src/root-config.js

## Overview

`src/root-config.js` is a javascript source file in the Application layer.

## Depends on

**`src`**

- `src/auth.js`
- `src/routing.js`
- `src/containers.js`
- `src/eventBus.js`

## Used by

Imported by 1 file in this repository.

**`public`**

- `public/index.html`

## Usage Notes

**Layer:** Application | **Role:** internal to its layer

## Questions this page answers

- What imports `src/root-config.js`?
- What does `src/root-config.js` depend on?

## In the code

name app publish subscribe single-spa ./auth ./routing ./containers ./eventBus /mfes.json no-store [root-config] failed to bootstrap applications import register application start from single spa init auth authenticated has any role get token login logout active routing create container containers event bus const window async function bootstrap await res fetch mfes json cache store throw new error failed load status text apps for system entry when location pathname custom props dom element getter catch err console root config applications

---

*Built from the code itself: parsed symbols, the import graph, git history and
the knowledge graph. Every statement here is checked against the source rather
than written about it.*